<?xml version="1.0"?>
<doc>
    <assembly>
        <name>Voba</name>
    </assembly>
    <members>
        <member name="T:Voba.AI.Interpreter.AllergyExpression">
             <summary>
             TERMINAL EXPRESSION — Food Allergy (Interpreter pattern).
            
             Takes a short allergen name (e.g. "seafood", "nuts") and expands it
             into every specific ingredient Gemma must avoid.
            
             This replaces the hardcoded FORBIDDEN rules previously written
             directly into the GemmaIdeationHandler prompt — those rules only
             covered "seafood" and "fish". This class covers all common allergens
             and is trivially extensible.
             </summary>
        </member>
        <member name="T:Voba.AI.Interpreter.AndExpression">
             <summary>
             NON-TERMINAL EXPRESSION — Combines restrictions (Interpreter pattern).
            
             When a user enters multiple restrictions (e.g. "Vegan, Nuts, Gluten-Free"),
             RestrictionParser wraps them in an AndExpression. Interpret() produces
             a single rule block with each restriction on its own line.
            
             Example output:
            
               DIET: Vegan — FORBIDDEN: all meat, poultry, fish, dairy, eggs, honey.
               ALLERGY — treat as DEADLY: NUTS — FORBIDDEN: peanuts, almonds, cashews...
               DIET: Gluten-Free — FORBIDDEN: wheat, barley, rye, regular flour...
             </summary>
        </member>
        <member name="T:Voba.AI.Interpreter.DietExpression">
             <summary>
             TERMINAL EXPRESSION — Lifestyle Diet (Interpreter pattern).
            
             Recognises named diets and maps each to a precise forbidden-ingredient
             rule that Gemma 3:1b can follow reliably.
            
             Adding a new diet means adding one entry to _dietRules — no prompt
             logic, no handler code, and no other class changes.
             </summary>
        </member>
        <member name="T:Voba.AI.Interpreter.IRestrictionExpression">
             <summary>
             ABSTRACT EXPRESSION (Interpreter pattern).
            
             Every dietary restriction — whether a lifestyle diet, a food allergy,
             a combination, or nothing at all — implements this interface.
            
             Interpret() produces a precise, Gemma-ready rule string that replaces
             the raw user input previously pasted directly into the prompt.
             </summary>
        </member>
        <member name="T:Voba.AI.Interpreter.RestrictionParser">
             <summary>
             CLIENT / PARSER (Interpreter pattern).
            
             Turns a raw List&lt;string&gt; from the UI into an expression tree.
             The tree is then evaluated by calling Interpret() to produce
             a structured rule block ready for the Gemma prompt.
            
             Classification logic lives here — not in the handlers, not in the prompt.
            
             Usage:
               var expression = RestrictionParser.Parse(context.DietaryRestrictions);
               string rules   = expression.Interpret();
               // Inject `rules` into prompt.
             </summary>
        </member>
        <member name="M:Voba.AI.Interpreter.RestrictionParser.Parse(System.Collections.Generic.List{System.String})">
            <summary>
            Parses the restriction list into an expression tree.
            Returns NoneExpression if the list is empty.
            Returns a single terminal if the list has one entry.
            Returns an AndExpression if the list has multiple entries.
            </summary>
        </member>
        <member name="T:Voba.AI.Interpreter.NoneExpression">
            <summary>
            TERMINAL EXPRESSION — No restrictions (Interpreter pattern).
            Used when the user left the dietary field blank.
            </summary>
        </member>
        <member name="T:Voba.AI.Interpreter.UnknownRestrictionExpression">
            <summary>
            TERMINAL EXPRESSION — Unrecognised input (Interpreter pattern).
            Fallback for any restriction the parser could not classify as a
            known diet or allergen. Passes the raw text to Gemma with a
            generic compliance instruction.
            </summary>
        </member>
        <member name="T:Voba.AI.Pipeline.Handlers.GemmaFullRecipeHandler">
             <summary>
             CHAIN LINK — Pipeline 2, Step 2 (Full Recipe).
            
             Receives a selected, priced RecipeConcept and asks Gemma to
             generate full step-by-step cooking instructions.
            
             Uses the same Interpreter as GemmaIdeationHandler so that
             dietary rules are enforced consistently across both prompts —
             the same expression tree, the same rule text.
             </summary>
        </member>
        <member name="T:Voba.Resource">
            <summary>
            Android Resource Designer class.
            Exposes the Android Resource designer assembly into the project Namespace.
            </summary>
        </member>
    </members>
</doc>
