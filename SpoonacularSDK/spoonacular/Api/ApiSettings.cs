namespace spoonacular.api
{
    // Committed placeholder — set the real key in ApiSettings.local.cs (gitignored).
    public static partial class ApiSettings
    {
        public static string SpoonacularApiKey { get; set; } = string.Empty;
    }
}
